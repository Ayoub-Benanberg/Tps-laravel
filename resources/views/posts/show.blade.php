{{-- @extends("layout")
@section("content")
    <div>
        @foreach ($posts as $post)
            @if ($post["id"] == $id)
                <h1>{{ $post["title"] }}</h1>
                <p><strong>Author:</strong> {{ $post["author"] }}</p>
                <p>{{ $post["content"] }}</p>
            @endif
        @endforeach
    </div>
@endsection --}}